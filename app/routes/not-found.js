import Ember from 'ember';
import AuthenticatedRouteMixin from 'simple-auth/mixins/unauthenticated-route-mixin';

export default Ember.Route.extend({
  setupController: function (controller, model) {
  }
});
